import styled from 'styled-components';

export default styled.div`
  display: flex;
  justify-content: center;
  transform: rotateZ(0) rotateX(33deg) rotateY(0deg) translateZ(-200px) translateX(0px) translateY(-250px);
  align-items: center;
  background: transparent;
  // position: fixed;
  // z-index: 1;
  // top: 0;
  // left: 0;
  // right: 0;
  // bottom: 0;
  margin-top: 25vh;
`;
